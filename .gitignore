# Secrets — never commit
.env

# Output files
output/

# Scratch logs + OS cruft
scratch_*.log
.DS_Store

# Python
__pycache__/
*.pyc
venv/
.venv/
.superpowers/sdd

# Auto-apply generated artifacts
auto-apply/drafts/
auto-apply/review_queue.csv
auto-apply/applications.csv
auto-apply/resume/resume.txt
auto-apply/resume/resume.pdf

# Phase 2: generated userscript (embeds Groq key + résumé) and shortlist
auto-apply/linkedin-easyapply.user.js
# Glob, not just shortlist.html: shortlists are generated per person
# (shortlist-<name>.html via --out), and each one is a named individual's job
# search. Generated + personal = never committed.
auto-apply/shortlist*.html
